config/config.js

# Build tooling — regenerated by `npm ci` in CI, never needs committing
node_modules/

# Local packaging output
output/
*.ipk

# Local-only dev cruft (never part of a webOS app)
.venv/
.claude/
